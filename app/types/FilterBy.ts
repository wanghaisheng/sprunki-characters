export enum FilterCharacters {
  status = 'status',
  species = 'species',
  gender = 'gender',
}

export enum FilterEpisodes {
  name = 'name',
  episode = 'episode',
}

export enum FilterLocations {
  name = 'name',
  type = 'type',
}
